name: napi-wasmer-quickjs

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  quickjs-native-linux:
    name: quickjs-native-linux
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: "true"
      CC: clang
      CXX: clang++
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Build native QuickJS N-API
        run: make build-napi-quickjs CMAKE_BUILD_TYPE=Release JOBS=4

      - name: Test native QuickJS N-API
        run: make test-napi-quickjs CMAKE_BUILD_TYPE=Release JOBS=4 TEST_JOBS=4

      - name: sccache stats
        if: always()
        run: sccache --show-stats || true

  quickjs-native-macos:
    name: quickjs-native-macos
    runs-on: macos-latest
    env:
      SCCACHE_GHA_ENABLED: "true"
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Build native QuickJS N-API
        run: make build-napi-quickjs CMAKE_BUILD_TYPE=Release JOBS=4

      - name: Test native QuickJS N-API
        run: make test-napi-quickjs CMAKE_BUILD_TYPE=Release JOBS=4 TEST_JOBS=4

      - name: sccache stats
        if: always()
        run: sccache --show-stats || true

